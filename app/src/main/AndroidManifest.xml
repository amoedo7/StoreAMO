<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />

    <!-- StoreAMO evita QUERY_ALL_PACKAGES. Consulta launchers y los handlers
         del instalador Android necesarios para entregar un APK verificado. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.INSTALL_PACKAGE" />
            <data android:mimeType="application/vnd.android.package-archive" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/vnd.android.package-archive" />
        </intent>
        <package android:name="com.termux" />
        <package android:name="com.desarrollamo.plataformamo.dev" />
        <package android:name="com.desarrollamo.storeamo.debug" />
    </queries>

    <application
        android:allowBackup="false"
        android:label="StoreAMO"
        android:icon="@drawable/ic_storeamo"
        android:roundIcon="@drawable/ic_storeamo"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.StoreAMO">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".util.InstallResultReceiver"
            android:exported="false" />

        <activity
            android:name=".InstallFlowActivity"
            android:exported="false"
            android:excludeFromRecents="true" />

        <activity
            android:name=".UninstallFlowActivity"
            android:exported="false"
            android:excludeFromRecents="true" />

        <activity
            android:name=".GoodNewsActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivityV3"
            android:exported="false" />

        <activity
            android:name=".BootstrapActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
