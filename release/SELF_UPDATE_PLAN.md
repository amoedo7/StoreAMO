# StoreAMO self-update

StoreAMO 0.4.3.84 routes its own verified APK through the same in-app DownloadInstaller/InstallFlowActivity path used for catalog applications. Android keeps the final install confirmation.
