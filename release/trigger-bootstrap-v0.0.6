Publish StoreAMO Install 0.0.6 after the Files still open PackageInstaller fix passes CI. This installer uses package com.desarrollamo.storeamo.installer so it can coexist with the broken 0.0.5 bootstrap.
